runtime: python311
entrypoint: gunicorn -b :$PORT app:app

automatic_scaling:
  min_instances: 0
  max_instances: 10

handlers:
  - url: /static
    static_dir: static
  - url: /icons
    static_dir: icons
  - url: /.*
    script: auto
